spring:
  profiles:
    active: local

  application:
    name: interview-platform

  datasource:
    url: jdbc:postgresql://localhost:5432/ai_mock_interview
    username: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true